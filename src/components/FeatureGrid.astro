---
interface Props {
  columns?: 2 | 3;
}

const { columns = 3 } = Astro.props;
const gridClass =
  columns === 2 ? "grid gap-4 md:grid-cols-2" : "grid gap-4 md:grid-cols-3";
---

<div class={gridClass}>
  <slot />
</div>
